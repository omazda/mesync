"""control — бэкенд mini-app «MAX ⇄ Telegram Sync».

Пакет содержит:
- store.py        — персистентное JSON-хранилище (аккаунты, правила, трафик,
                    уведомления, подписки, коды привязки, OTP) с асинхронным I/O;
- security.py     — проверка подписи хоста (MAX requestContact / Telegram initData),
                    JWT-сессии, генерация/проверка OTP;
- sources.py      — единый список источников поверх ownership обоих ботов и выдача
                    кодов привязки, инициированных из mini-app;
- rules.py        — CRUD правил + обогащение из ownership + движок маршрутизации;
- api.py          — FastAPI-приложение со всеми эндпоинтами mini-app.

Вся работа с сетью/файлами — async (требование проекта).
"""
